source "https://rubygems.org"

# GitHub Pages runs Jekyll with a vetted dependency set.
# Using this gem makes local builds match Pages as closely as possible.
gem "github-pages", group: :jekyll_plugins

group :development do
  gem "webrick"
end
